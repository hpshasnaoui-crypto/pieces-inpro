name: Deploiement GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Permissions requises par actions/deploy-pages
permissions:
  contents: read
  pages: write
  id-token: write

# Une seule publication a la fois ; on laisse finir celle en cours.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build

      # enablement: active GitHub Pages automatiquement s'il ne l'est pas
      # deja, au lieu d'echouer sur "Get Pages site failed".
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
